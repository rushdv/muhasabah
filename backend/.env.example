# Backend example env. Copy to `.env` and set values.
# Database URL example (already present in your repo's .env):
# DATABASE_URL=postgresql://postgres:password@localhost:5432/muhasabah

# Google client id (optional) - should match the frontend VITE_GOOGLE_CLIENT_ID
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
