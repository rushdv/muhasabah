{
  "name": "muhasabah-monorepo",
  "private": true,
  "workspaces": [
    "backend-node",
    "frontend"
  ],
  "scripts": {
    "build": "npm run build --workspace=frontend",
    "deploy": "vercel --prod"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.3.3",
    "@types/node": "^20.10.5"
  }
}